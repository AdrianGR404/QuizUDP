<UserControl x:Class="Servidor.Views.Puntuaciones"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Servidor.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#FF2D2D30">
        <Border Background="#FF1E1E1E" CornerRadius="15" Margin="20" Padding="30" BorderThickness="1" BorderBrush="#FF3E3E42">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="5" Color="Black" Opacity="0.5" BlurRadius="10"/>
            </Border.Effect>
            <StackPanel>
                <TextBlock Text="Resultados del Quiz" 
                           FontSize="28" 
                           TextAlignment="Center" 
                           Margin="0,0,0,20" 
                           Foreground="#FF007ACC" 
                           FontWeight="Bold"/>

                <Border Background="#FF171717" 
                        CornerRadius="10" 
                        Padding="20" 
                        Margin="0,0,0,20">
                    <Border.Effect>
                        <DropShadowEffect ShadowDepth="3" Color="Black" Opacity="0.3" BlurRadius="5"/>
                    </Border.Effect>
                    <ListBox ItemsSource="{Binding Alumnos}" 
                             Background="Transparent" 
                             BorderThickness="0" 
                             Padding="0">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#FF2D2D30" 
                                        CornerRadius="8" 
                                        Margin="0,5" 
                                        Padding="15">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="300"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        
                                        <TextBlock Text="{Binding NombreCompleto}" 
                                                   FontSize="18" 
                                                   Foreground="White" 
                                                   FontWeight="SemiBold"/>
                                        
                                        <TextBlock Text="{Binding Puntuacion}" 
                                                   Grid.Column="1"
                                                   FontSize="18" 
                                                   Foreground="#FF00CC99" 
                                                   FontWeight="Bold" 
                                                   Margin="20,0"/>
                                        
                                        <ItemsControl Grid.Column="2" 
                                                      ItemsSource="{Binding Respuestas}" 
                                                      Margin="10,0,0,0">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <StackPanel Orientation="Horizontal"/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border Background="#FF3E3E42" 
                                                            CornerRadius="5" 
                                                            Margin="5,0" 
                                                            Padding="8,4">
                                                        <TextBlock Text="{Binding}" 
                                                                   Foreground="White" 
                                                                   FontSize="14"/>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Border>

                <Button Content="Regresar al Quiz" 
                        Command="{Binding RegresarCommand}" 
                        Width="250" 
                        Margin="0,10,0,0" 
                        FontSize="18" 
                        Background="#FF007ACC" 
                        Foreground="White" 
                        BorderBrush="#FF005A9E" 
                        Padding="15" 
                        HorizontalAlignment="Center" 
                        Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="1"
                                                CornerRadius="12">
                                            <ContentPresenter HorizontalAlignment="Center" 
                                                            VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#FF1E90FF"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <Button.Effect>
                        <DropShadowEffect ShadowDepth="3" Color="Black" Opacity="0.3" BlurRadius="5"/>
                    </Button.Effect>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>